platform: linux

image_resource:
  type: docker-image
  source: {repository: niaquinto/gradle}

inputs:
  - name: resource-filterCarList

outputs:
  - name: resource-jar

run:
  path: ./resource-filterCarList/ci/scripts/package.sh

